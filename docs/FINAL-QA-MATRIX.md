# Final QA Matrix

> **AUTHORITATIVE ENVIRONMENT DECISION (supersedes every earlier VPS / PHP-upgrade / DNS framing).**
>
> **Environment.** The CRM runs on the **current cPanel hosting with PHP 8.1.34**, and that is the
> **approved environment** for the initial **2–3 internal users**. This installation is the intended
> home for that limited internal use — not a throwaway box that gets replaced before go-live. It is
> today in **pre-live internal use**: code deployed, database holding only configuration and
> synthetic records, **no real patient data**. It becomes **limited internal production use** once
> the owner clears the privacy/KVKK gate and the manual UI QA. Wherever an older document says
> "staging only", read it as *pre-live internal use on the installation that will become limited
> internal production*; the terminology is unified on that transition, not on two conflicting labels.
>
> **PHP.** **8.1.34 is the production runtime.** There is **no PHP 8.3 production target.** PHP
> **8.3.33 and 8.4 static syntax lint passed** for all 64 module files — that is a
> forward-compatibility data point only, and the **8.3 application runtime was never verified**
> (the php83 CLI also emits an `nd_mysqli.so` undefined-symbol startup warning, a CLI
> extension-config artifact, not an app error). **VPS migration, PHP 8.3/8.4 adoption, DNS
> migration, load balancing, containers and Redis are optional future work — not go-live
> requirements** — and none of them appear in the immediate owner checklist.
>
> **Disk — two separate facts; do not conflate them.**
> 1. *Hosting-provider condition (monitored, NOT an account-capacity blocker):* the shared array
>    `/dev/md3` is **95% full — 4.7 TB used of 5.2 TB, 284 GB free**. It is the provider's volume,
>    shared across accounts. The owner cannot resize it and it does not cap this account. Monitor it
>    and raise it with the host.
> 2. *This account's own usage vs quota (ample headroom):* **16,362 MB used of a 51,200 MB quota
>    (~32%; 34,838 MB free)** and **295,019 of 500,000 inodes (~59%)**. Measured 2026-08-29 via
>    cPanel `Quota::get_quota_info`. Each DB dump adds well under 1 MB.
>
> **Error logs.** The earlier truncation removed only PHP **8.3 CLI lint** warnings generated by this
> work — never application errors. Those CLI warnings were **rotated** (timestamped, checksummed,
> gitignored) and the **application itself subsequently generated no new error log**. Rotated logs
> are now stored **outside the document root**, mode `600`.
>
> **Versions — module version ≠ schema version.** `se_core` **module v1.0.0**, **database schema
> v7**. `se_appointments` **module v1.0.0**, **database schema v2**. `se_whatsapp` **module v1.0.0**.
> `perfex_dark_theme` **v1.2.3** (vendor). Earlier text that wrote "se_core v7 / se_appointments v2"
> as *module* versions was wrong.
>
> **Reviewed final commit:** `c60d39d4fa7c5e16344c2f833bca5a8f7b77c592`.



Re-verified at `main` = **`c60d39d4fa7c5e16344c2f833bca5a8f7b77c592`** on the cPanel installation
(pre-live internal use; PHP 8.1.34).

**Test-session provenance — read this before weighing any row below.**
- **Earlier phases (0, 2–6):** HTTP integration results were obtained using **temporary synthetic database
  sessions** — short-lived rows inserted into `tblsessions` for synthetic staff accounts, driven with the
  resulting cookie, then deleted (teardown verified at 0 rows). Real HTTP evidence, fabricated sessions.
- **Phases 8 and 9: no session was fabricated.** Regression there is network-free unit suites, direct
  DB-level assertions and security scans only.
- **Therefore no authenticated browser UI check has ever been performed by a human.** Every row whose
  evidence is "visual" or "authenticated UI" is genuinely pending, not merely unrecorded.

## Re-run in Phase 8 (this session)
| Check | Result |
|-------|--------|
| PHP 8.1 lint (all modules) | PASS |
| PHP 8.3.33 + 8.4 static lint (64 files) | PASS |
| Migration idempotency (reset+re-apply; IF NOT EXISTS) | PASS — schema v7, 138 tables, no drift |
| Meta Lead Ads unit (`phase4_unit`) | 21/0 |
| Google Data Manager unit (`phase5_unit`) | 33/0 |
| Reporting importer unit (`phase6_unit`) | 7/0 |
| Brand isolation (scope predicate, DB) | PASS (own=1, foreign=0, admin=2) |
| Security scans (secrets/residue/CSRF/perms/plugins) | PASS |
| Cron + app smoke | cron 200, login 200 |
| Error-log regression | Only PHP 8.3 **CLI lint** warnings were ever produced; they were **rotated** (not truncated as app evidence). The **application generated no new error log** afterwards. |
| Rotated-log exposure | **FAILED then FIXED** — `error_log.claude-php83lint-20260829_093518` was inside the document root, mode `644`, and served **HTTP 200**. Moved to `~/_evidence/logs/`, mode `600`, off document root; old URL now **404**. |
| Webhook processing latency | **BLOCKER** — cron is `3-59/15` (every 15 min); the Perfex 300 s throttle never blocks it. Effective worst case ≈ **15 min**, not 5. |
| Credential storage | **BLOCKER for live integration** — every ad-platform secret is designed to sit as plaintext in `tbloptions`; zero encryption in the custom modules. No credential is currently populated. |
| Google Data Manager authentication | **BLOCKER** — static bearer token in option `se_google_sa_token_<brand>`; no service-account/ADC token renewal. Google access tokens expire hourly. |
| Conversion attribution snapshot | **DEFECT** — senders rebuild historical conversions from the lead's current mutable row; the `payload` snapshot column is written as `[]` and never read. |
| Account quota vs shared array | account **16,362 / 51,200 MB (~32%)**, inodes **295,019 / 500,000 (~59%)**; shared array `/dev/md3` **95%** = monitored hosting-provider condition, not an account blocker. |

## Component classification (exactly one status each)
| Component | Status | Evidence |
|-----------|--------|----------|
| Brand isolation & authorization | Live and end-to-end verified | HTTP cross-brand deny (P0/P2) + DB scope re-verified (P8) |
| Attribution first/last-touch + fbc/fbp/ctwa | Functional with fixtures | unit 30/0; live web-to-lead form run pending |
| Consent ledger | Functional with fixtures | unit + isolation |
| Patient records (schema + API) | Functional with fixtures | tables + API + access log; brand-scoped |
| Pipeline (13 stages) | Live and end-to-end verified | seeded live; producer + lost/junk gating 13/0 |
| Conversion outbox (dedup/atomic-claim/lease/retry/windows) | Functional with fixtures | 2-worker disjoint; live send gated |
| Appointments (lifecycle/overlap/hours/tz/reschedule/cancel/no-show/history/reminder-dedup) | Live and end-to-end verified | real edit-form HTTP 19/0 (in-phase) + model unit 13/0 |
| Appointment reminder queue | Functional with fixtures | enqueue/cancel/reschedule proven; consumer gated on WhatsApp |
| Google Calendar sync | Externally gated (fixture-only) | fixture adapter idempotent; live needs service account |
| WhatsApp processing (signature/routing/wamid-dedup/status-order/unknown-parking/metering) | Functional with fixtures | unit 13/0 + cron 11/0 |
| WhatsApp inbox UI + brand scoping | Functional with fixtures | routes render; isolation verified; visual = manual pending |
| WhatsApp live Meta connection + public POST | Externally gated | token/number/webhook subscribe + csrf exception (owner) |
| Meta Lead Ads inbound (webhook/bigint/dedup/routing/mapping/consent) | Functional with fixtures | unit 21/0 + cron 5/0; GET verify live |
| Meta Lead Ads live ingestion | Externally gated | leads_retrieval Advanced Access + Page token |
| Meta CAPI payload + sender | Functional with fixtures | 31/0 payload + 9/0 gating |
| Meta CAPI live transmission | Externally gated | dataset + system-user token |
| Google Data Manager sender | Functional with fixtures | unit 33/0; cron gated-hold |
| Google Data Manager live delivery | Externally gated | Cloud service account + conversion actions |
| WhatsApp landing-token attribution | Functional with fixtures | create/verify/tamper/expiry/apply |
| Reporting internal metrics + dashboard + health routes | Live and end-to-end verified | JSON endpoints 21/0; render 200 |
| Reporting external imports (GA4/GSC/Ads) | Externally gated | credentials required; importer framework fixture 7/0 |
| Dark Theme | **Installed and functionally activated — visual/responsive QA pending** | `perfex_dark_theme` v1.2.3 `active=1` in `tblmodules`; activation idempotent and non-destructive. **No authenticated visual or responsive check performed — not end-to-end verified.** |
| Cron + outbox drain wiring | Live and end-to-end verified | after_cron_run drains; gated holds; no external call |
| DNS/TLS/VPS production cutover | Externally gated (prepared, not executed) | runbooks only |
| App Review submissions (Meta) | Externally gated (not submitted) | readiness packs prepared |

## Manual QA pending (owner; requires a real authenticated login — no session has ever been forged for this)

**Until these are complete, Patient CRUD UI and Dark Theme stay at their pending classifications above and
must not be called end-to-end verified.**

- **Patient CRUD UI:** authenticated list/search/pagination, create, view, edit, archive — plus
  **authorization QA**: a staff member without the `se_patients` capability is denied; a staff member scoped
  to Brand A cannot open a Brand B patient by id.
- Visual dark-mode rendering across key admin screens, including responsive/mobile widths.
- Reporting dashboard + integration-health page visual review (`/admin/se_core/se_reports/index` and `/health`).
- WhatsApp inbox + conversation views; appointment create/edit forms (new fields + filters).
- Lead profile: WhatsApp + appointment tabs render.

## Phase 9 additions
| Component | Status | Evidence |
|-----------|--------|----------|
| Patient CRUD UI (list/search/pagination/create/view/edit/archive) | **Functional with fixtures — automated model/DB tests passed; authenticated UI and permission QA pending** | unit 19/0 + DB 7/0 (brand isolation, cross-brand ID denial, archive, linkage, consent/audit). **No authenticated browser check of the UI or of the Perfex capability gates has been performed.** |
| Patient conversion-data prohibition | Live and end-to-end verified | CAPI + Google DM payloads carry no patient/clinical field; builders are lead-only |
| Infrastructure (cPanel + PHP 8.1.34) | **Approved environment — production runtime** | Suitable for 2–3 internal users. PHP 8.1.34 is the production runtime; there is **no PHP 8.3 production target**. |
| VPS / PHP 8.3 / DNS migration | **Optional future work — not a go-live requirement** | Removed from the immediate owner checklist. 8.3/8.4 **static lint only**; 8.3 runtime never verified. |
| Shared filesystem at 95% | **Monitored hosting-provider condition — not an account-capacity blocker** | `/dev/md3`, 4.7 TB of 5.2 TB used, 284 GB free; shared across accounts, owner cannot resize. |
| This account's usage vs quota | **Ample headroom** | **16,362 / 51,200 MB (~32%)**, **295,019 / 500,000 inodes (~59%)** — cPanel `Quota::get_quota_info`, 2026-08-29. |
| Backup verification | **Dump integrity evidence only — restore NOT proven** | Completion marker present; 138 `CREATE TABLE` == 138 live base tables; header parses. **No restore has ever been performed and application recovery is unproven.** |
| Privacy / KVKK launch gate | **Open — required before any real patient data** | Owner + qualified Turkish privacy counsel; see `docs/CRM-A-Z-FINAL-REPORT.md` §15. No compliance is claimed. |

## Classification enum used across all docs

`Live and end-to-end verified` · `Functional with fixtures` · `Functional with fixtures — automated
model/DB tests passed; authenticated UI and permission QA pending` · `Installed and functionally activated
— visual/responsive QA pending` · `Externally gated` · `Prepared but not executed` · `Optional future work`

**Retired:** `Scaffold/partial` (the Patient UI row that used it is removed — the UI is built, not a
scaffold; what is missing is authenticated QA) and `Optional future roadmap` (renamed `Optional future
work`).

**Rule:** a component whose manual UI checks are incomplete is **never** described as end-to-end verified.
