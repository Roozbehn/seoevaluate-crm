# CRM Source Conflicts — 2026-09-04

Precedence used: (1) current code/live behaviour = CURRENT STATE, (2) UIUX-OPT = target UI/UX, (3) DESIGN-SYSTEM = shared components/tokens, (4) UX-COPY = staff-facing Turkish, (5) CRM-AUDIT = security/reliability/backend unless superseded by current code, (6) legal/clinical/business constraints override everything.

| # | Source A | Source B | Current code evidence | Decision | Reasoning | Resulting implementation |
|---|---|---|---|---|---|---|
| 1 | CRM-AUDIT App.1 §2: canonical term for lead = **Aday** | UX-COPY §1: the person is always **Hasta**, stage 1 = *Yeni talep*; *Aday* retired | `se_appointments_lang` "Potansiyel müşteri", `se_journey_lang` "Fırsat", tests reference "lead" | **UX-COPY wins** (precedence 4) | UX-COPY is the later, dedicated terminology document; audit appendix was a first pass | All staff strings use *Hasta / Yeni talep*; "Aday" does not appear. Audit App.1 rows 2–4 are marked satisfied by the UX-COPY versions (traceability App1 → CRM-M018). |
| 2 | CRM-AUDIT §C.11 / App.1 #5: `Görüşme` → "Sohbet" for chat, keep *Görüşme* for consultation | UX-COPY §1: consultation = **Ön görüşme** (never bare *Görüşme*) | lang keys use "Ön görüşme" in journey, "Görüşme" in whatsapp | UX-COPY wins | consistency: bare *Görüşme* stays ambiguous | Chat = Sohbet; consultation = Ön görüşme everywhere; *Görüşme yapıldı* is allowed only as the shortened button on an appointment row whose type is already shown. |
| 3 | CRM-AUDIT AZCRM-UX-001: "Needs me today" list on the **dashboard** | UIUX-OPT §D: dashboard renamed **Bugün**; UIUX-BACKLOG D01 | `Se_dashboard.php` renders counters | Same implementation (CRM-M023) | Different layers of one fix | One controller; both IDs map to CRM-M023. |
| 4 | CRM-AUDIT AZCRM-UX-005: keep dashboard counters as *today/this week* | UIUX-OPT §D: **remove** all-time counters; pipeline pills only | — | UIUX-OPT wins | Later target UX | Counters removed; "Hasta akışı · bu hafta" pills carry the weekly numbers; brand-0 fix retained. |
| 5 | CRM-AUDIT AZCRM-WF-004: consultation auto-held **and** "Book procedure now" in one ticket | UIUX-BACKLOG A04 (shortcut, UI) vs WF-002 timers (auto-held is automation) | `consultation.php:170-214` marks held only by staff/cron-when-appointment-status set | Split into CRM-M041 (shortcut) + CRM-M047 (auto-held) | Different risk classes: UI vs automation touching state | Shortcut shipped in Wave 6; auto-held is Wave 7 with its own test. |
| 6 | CRM-AUDIT F2: "grant `ads` alongside `marketing` if lawyer agrees" | Program rule §17: never fabricate legal approval | `intake.php:766-779` grants health_data/marketing/photo_publication only | **BLOCKED-DECISION** | No approval on record | CRM-M010 adds an explicit brand option `se_consent_ads_from_intake_<brand>` (default **off**), Health shows *Blocked: no ad consent from WhatsApp intake — N rows* while off; a test proves both branches. Roozbeh/lawyer flip the option. |
| 7 | UIUX-BACKLOG says backend items "stay in AZCRM-*" | Program §2: nothing may be dropped | — | Both kept | — | Traceability lists all 67 AZCRM tickets and all 50 UX tickets; master tickets carry both IDs. |
| 8 | DESIGN-SYSTEM §1.1 primary `#4f5bf0` | Live theme primary `#5b67ff` (Perfex theme_style) | `getComputedStyle(.btn-primary)` = rgb(91,103,255) | DESIGN-SYSTEM wins for `.se-*` components; Perfex core buttons keep the theme colour | White on #5b67ff is 4.35:1 (fails AA); DS token is 5.1:1 | `se-ds.css` uses the DS token; `body.se-clinic .btn-primary` is **not** overridden (core screens untouched, per "minimal Perfex change"). |
| 9 | UIUX-OPT §C: Perfex top-bar icons hidden behind chevron at ≤990 px | First audit observed overlap at 769 px in a real window; iframe probe at 800–990 did not reproduce | Perfex JS decides icon collapse by window width | Implement the CSS rule for 700–990 px regardless | Cheap, harmless when not needed | `body.se-clinic` media rule in se-ds.css; verified live at 769. |
| 10 | CRM-AUDIT §E state model: ➕ `quote_expired`, ➕ `long_term_followup`, ➖ pipeline stages | UX-COPY §3.2 lists `(new) quote_expired` but no long-term state | `se_journey_states()` has 31 states | Add `quote_expired` now (both sources); `long_term_followup` deferred (CRM-M056, needs protocol) | Only the state both documents agree on is added in this program | State machine +1 state, tests updated. |
| 11 | CRM-AUDIT App.1 #13 "Görüşme planlanacak → split" | UX-COPY §8 same split | `health.php:135` | Same | — | Counter split into "Teklif yanıtı bekleniyor" / "Ön görüşme planlanacak" inside Bugün pipeline/queue. |
| 12 | UIUX-OPT §B: Perfex Leads/Customers **hidden** for clinic roles | CRM-AUDIT AZCRM-UX-006 / MOB-003: **clean up** Leads fields and mobile pills | `se_clinic.php` hidden slugs | Both: hide for clinic roles (CRM-M021) now; admin cleanup (CRM-M031) planned, needs owner confirmation of pipeline stages | Owner must confirm stage trimming (business rule) | MOB-003 becomes moot for clinic roles; kept PLANNED under M031 for admins. |
| 13 | Program §8 Wave 1 lists "mobile WhatsApp composer critical fix" | Program §8 Wave 2 says CSS must come from se-ds.css, not a patch | `se_chat_ui.php` inline CSS | Do the composer fix **inside** se-ds.css in Wave 2, but ship Wave 1+2 together | Avoids a throwaway patch and transitional UX | Composer fix = CRM-M032 delivered with CRM-M015. |
| 14 | CRM-AUDIT: appointments reminder consumer should use journey copy when journey exists | UX-COPY §0: patient-facing copy untouched | `messaging.php:785` template `eyebrow_consultation_reminder_tr` exists and is approved | Use the existing approved template only; no new patient copy | Rule 4 | CRM-M002 passes `template_ref` = existing template; variables from the appointment. |
| 15 | CRM-AUDIT §G recommends `SeQueue` shared drainer (WS15) | Program §8 Wave 10: only after behaviour is protected by tests; no rewrite for purity | six drainers pass their tests | PLANNED, not executed in this program | Risk/benefit at 2–3 users | CRM-M065 stays PLANNED with justification. |
