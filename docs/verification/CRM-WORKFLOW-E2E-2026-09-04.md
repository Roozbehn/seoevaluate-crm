# CRM Workflow E2E — 2026-09-04

**Where it ran.** The fake-DB harness (`php modules/se_core/tests/run.php`) with the **fake WhatsApp transport** (`se_wa_register_transport(fixture)`) and the sandbox brand flag. Every journey below is a real code path — webhook envelope → listener → state machine → outbox → transport fixture → mirror → next action → Bugün/Hastalar/Mesajlar rows — with no message leaving the machine. **No test message was sent to any real patient, no integration was toggled on production, no production data was touched** (program rules §22).

**Why not the production sandbox brand.** A walk on the live host would either send real WhatsApp messages (forbidden) or run the same code against the same fixtures the harness already runs, with the added risk of writing rows into production tables. The harness gives the same evidence, isolated, in 4 380 assertions.

Legend: ✅ passes in the suite (group name quoted) · ⚠️ partially covered · ❌ not covered.

| Journey | Steps exercised | Evidence (suite → group) | Result |
|---|---|---|---|
| **A · New WhatsApp enquiry → welcome** | inbound envelope (signature, dedup, variants, CTWA referral) → lead reuse/creation → ONE journey → welcome queued → transport fixture → mirrored `origin=journey:welcome` → Bugün row "Yeni talep" | `journey` → "exact pre-filled message creates ONE lead + journey and ONE welcome", "duplicate webhook delivery is fully idempotent", "punctuation/case/whitespace variants…", "Click-to-WhatsApp referral…", "an existing lead with the same phone is reused"; `whatsapp` → "Duplicate envelope does not inflate unread…"; `today` → "Bugün queue: scope, filtering, ordering" (`unread_no_journey`, `new`) | ✅ |
| **B · Organic / website enquiry → staff start** | organic waits for staff; website lead auto-start behind its switch; Start from thread; Start from lead; welcome via start template outside the window | `journey` → "organic enquiries wait for staff"; `journey_autostart` (4 groups); `journey_templates` → "Start from the WhatsApp thread…", "Start from a LEAD…", "the welcome uses the start template outside the window" | ✅ |
| **C · Consent → intake → photos** | privacy notice + secure link; consent as a hard gate; intake autosave/validation/flags/encryption; token rotation & isolation; photos validated, sealed, counted; retake with coded reason; Flow endpoint stores through the same functions | `journey` → "health-data consent text is a hard gate…", "start → privacy notice + secure link…", "intake autosave…", "token rotation…", "photographs via WhatsApp…"; `journey_flows` → "the endpoint walks the intake…"; `journey_staff` → "photo classification, accept, retake…"; `journey_r2` (sealed storage) | ✅ |
| **D · Review → decision → quote → patient answer** | default-deny on health/photos; human decision (flags never auto-reject); quote needs an approver; sent quote carries 3 buttons; accept → `quote_accepted` + calendar link; revision → task, new version, late accept; quote straight from `ready_for_review`; expiry → `quote_expired` by the system, late accept still counts | `journey_staff` → "health answers and photos are default-deny", "review decisions are human…", "quote cannot be sent until an authorised staff member approves it"; `journey_quote` (9 groups); `journey_timers` → "Quote expiry: a late patient answer still counts" | ✅ |
| **E · Booking → confirmation → reschedule → reminder** | booking link; patient self-books through the real model (working hours minus existing, notice/horizon); staff booking with conflict refusal + human message + next free slot; confirmation once per appointment+start; **reschedule sends a fresh confirmation**; reminder T-24h/T-72h consumer loads by appointment id and skips cancelled | `journey_quote` → "the calendar…", "the patient books a slot…"; `journey_staff` → "consultation booking through the real model — no double booking" (+ CRM-M044 block); `appointments` → "Conflict message and next free slot"; `wa_outbound` → "Reminder consumer (appointment module) — CRM-M002"; `journey` → "reminders — one after 24h…" | ✅ |
| **F · Consultation held → outcome → procedure booked → pre-op** | held/no-show/cancelled reflect on the journey with tasks; procedure booking; pre-op gate; completion | `journey_staff` → "consultation booking…" (outcome part), "procedure booking, pre-op gate, completion, aftercare"; `journey_timers` → held-unrecorded nudge | ✅ |
| **G · Procedure completed → aftercare → follow-up → completed** | plan start (manual), unapproved protocol → tasks only; approved protocol → auto-start within one cron tick; check-ins, photo requests, urgent answer pauses automation and alerts (no diagnosis); completion | `journey_staff` → "…completion, aftercare"; `journey` → "urgent aftercare answer pauses automation and alerts — no diagnosis"; `journey_timers` → "Aftercare auto-start only with an approved protocol" | ✅ |
| **H · Opt-out / handoff / pause / resume** | STOP keywords stop non-essential automation, confirmed once; handoff pauses and alerts; staff reply = takeover (pause is opt-in from the composer); resume; reactivation after opt-out needs evidence | `journey` → "opt-out keywords…", "human handoff pauses automation…", "a staff reply from the composer is a takeover" (+ CRM-M006 opt-in assertions); `inbox` → "Contextual actions by state" (Resume offered when paused) | ✅ |
| **I · Closed / not suitable → reopen; lead deleted → relink** | reopen allowed by the machine with a reason; deleted CRM lead detaches with a task and re-links when the number returns; a second journey on the same thread gets its own welcome | `journey_relink` (4 groups); reopen: transition map + `Se_journey::action('reopen')` (controller, outside the harness — checked by lint and live) | ⚠️ controller path live-checked only |
| **Cross-cutting · Windows, templates, quiet hours, caps, delivery states** | outside the window only APPROVED templates (visible block otherwise); Flow-button template cannot be sent as plain; delivery/read/failure callbacks; quiet hours + daily cap; window re-checked at send time; one mirror per send; **no double send after a crash** | `journey` → "outside the service window…", "delivery / read / failure callbacks…", "quiet hours defer…"; `journey_flows` → "the composer cannot send a FLOW-button template…"; `wa_outbound` (12 groups incl. CRM-M058) | ✅ |
| **Cross-cutting · Screens fed by the same engine** | Bugün rows, Hastalar rows, Mesajlar rows and the workspace all derive from `se_journey_batch_context()` + `se_journey_next_action()` | `next_action` (table of 20 rows × 5 fields), `today`, `hastalar`, `inbox`, `workspace` | ✅ |

**Totals at the end of Wave 10:** 41 suites · **4 380 assertions · 0 failures** (baseline 3 824 → +556). Copy gate (`scripts/check-copy.sh`) OK on every wave.

**Not covered by the harness (by design):** controllers/views (CodeIgniter runtime) — covered by PHP lint on every changed file and the live checks in `CRM-UIUX-VISUAL-VERIFICATION-2026-09-04.md`; the Meta Flow JSON (CRM-M029); real Cloud API responses (transport is a fixture; the live transport is credential-gated and untouched).
