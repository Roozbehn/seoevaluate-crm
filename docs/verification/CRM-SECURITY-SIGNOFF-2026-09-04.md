# CRM Security Sign-off — 2026-09-04 (final closure)

Scope: the security items named by the closure program, each checked on the deployed CRM (crm.roozbeh.com.tr, `main` = final SHA in the production sign-off) or in the harness where a live check would require writing production data. Cookie/token values were never recorded; every host command that touched credentials wrote them to a 0600 temp file that was deleted in the same command.

| # | Item | How verified | Result |
|---|---|---|---|
| 1 | CSRF protection on; `?x=gateways/…` does **not** disable it | Harness `test_csrf.php` (exemption anchored to the real `/gateways/` path, query-string and mid-path variants refused). Live: POST `…/se_journey/action/6/note?x=gateways/stripe` without a token → **403**; the same POST with the page's `csrfData` token → accepted (redirect; empty note refused by validation, nothing written) | **PASS** |
| 2 | PWA push subscribe requires CSRF; unsubscribe is ownership-scoped | Live: POST `se_pwa/subscribe` and `se_pwa/unsubscribe` without a token → **403**. Harness `test_push.php`: the staff id comes from the session, never the body; "a different staff id cannot unsubscribe someone else's device"; endpoints restricted to known push services | **PASS** |
| 3 | Protected files return 404 | Live (unauthenticated fetch): `/docs/` 404 · `/.env` 404 · `/.env.example` 404 · `/services/` 404 · `/scripts/ui-regression/README.md` 404 · `/modules/se_core/tests/run.php` 403 · `/application/config/app-config.php` 403 · `/error_log` 403 (`.htaccess` deny) | **PASS** |
| 4 | Session cookie: Secure, HttpOnly, SameSite; login/logout | Host `app-config.php` now defines `APP_COOKIE_SECURE=true` and `APP_COOKIE_HTTPONLY=true` (backup `~/backups/app-config.php.pre-cookie-20260904-012718`; `APP_SESSION_COOKIE_SAME_SITE='Lax'` was already set). Observed on the host through a throwaway local PHP server: `csrf_cookie_name` and `sp_session` are `secure; HttpOnly; SameSite=Lax`. In the browser `document.cookie` no longer exposes the session cookie. Session persisted across the change; Bugün/Hastalar/Mesajlar/Randevular load; CSRF POST works; no redirect loop; no PHP error in the host log. A **fresh login** was not performed by the program (no credentials are ever typed) — owner to confirm once (DEC-006) | **PASS** (fresh login: owner confirmation) |
| 5 | Webhook signature verification; duplicate deliveries idempotent | Harness: "Journey: invalid signature is rejected and audited safely", "Instagram signature verification", "a verify-token file is NOT webhook verification", "Journey: duplicate webhook delivery is fully idempotent" (unread/window not inflated), Lead Ads redelivery does not re-fire `lead_created`. Host: 235 webhook events processed, 0 stuck | **PASS** |
| 6 | crm-media Worker fails closed | Node smoke against `services/crm-media/src/index.js`: no key → 503; short key → 503; empty/wrong bearer → 404; empty signature → 404 `bad_sig`; expired → 404; path traversal / outside prefix → 404 `bad_key`. The deployed Worker `crm-media` is the same source (Cloudflare account listing) | **PASS** |
| 7 | Assignment brand/role boundaries | Harness: "Assignment: an assignee must belong to the brand (CRM-M012 / audit C3)", "Brand authorization on the posted brand_id", "Staff selectors offer only in-brand staff", "Status history is brand-scoped", Hastalar/inbox "scope fail-closed" (unmapped staff sees nothing) | **PASS** |
| 8 | No secrets committed; no tokens in docs | `git grep` for Meta/GitHub/Stripe token shapes and private-key headers across the tree → only vendor CA bundles and base64 images; `git ls-files` has no `app-config.php`, `.env`, `state.json`, dumps or tarballs; `scripts/ui-regression/state.json` and `out/` are git-ignored. Host: SE secrets are files in `~/_secrets` (0600), **none** in `tbloptions`; the option-table fallbacks were removed from the code (H.L8 / SEC-006) | **PASS** |
| 9 | Real client IP behind Cloudflare (`proxy_ips`) | `config.php` carries the Cloudflare ranges (`APP_PROXY_IPS` override). Not re-verified against a fresh audit row in this pass (a visitor IP appears only when a staff member logs in from a new address) | **PASS** (config) / observation pending |
| 10 | Rate limits / cron key | `Cron.php` uses `hash_equals`; the cron key is read from the config by the host scripts at run time. The key value was displayed once in the working session's tool output (not in any file or commit) → rotation recommended (DEC-007) | **PASS** with a precaution |

**Not in scope / not claimed:** penetration testing, dependency CVE audit of Perfex core, WAF rules. This is a regression sign-off of the items the program set out to close, not a certification.
